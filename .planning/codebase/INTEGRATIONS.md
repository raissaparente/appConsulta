# Integrations

## Firebase
- Utilized for backend services and data persistence.
- `src/services/firebase.ts` handles the initialization and connection.
- Integrated into various services (`consultaService.ts`, `pacienteService.ts`, `medicoService.ts`) for data retrieval and manipulation.
