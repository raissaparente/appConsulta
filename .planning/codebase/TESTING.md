# Testing

- Currently, there are no visible testing frameworks configured in `package.json` (such as Jest or React Native Testing Library).
- Testing appears to be conducted manually or through Expo Go during the development cycle.
- The project includes a robust set of mock data in `src/data/`, which can be highly beneficial for implementing future unit and integration testing.
